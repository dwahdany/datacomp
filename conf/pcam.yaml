defaults:
  - upsample_base
  - _self_
gpu_id: 4
# indistribution_data_sampling_rate: 0.01
indistribution_data_tar_upsample: 1
output_dir: ${base_output_dir}pcam_${indistribution_data_tar_upsample}/datacomp_v${seed}/
seed: 0
indistribution_data_tar: '/datasets/pcam/shards/pcam-train-{000000..000262}.tar'
curation_task: 'pcam'